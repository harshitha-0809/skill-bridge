# Local development — Vite proxy handles /api, so leave this commented out
# VITE_API_URL=http://localhost:8000

# Production — set this in Vercel dashboard to your Render backend URL, e.g.:
# VITE_API_URL=https://skillbridge-api.onrender.com
